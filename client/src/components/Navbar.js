
// export default Navbar;
// // follow example and tweak it.

import { Link } from 'react-router-dom'
import SignUp from "./Signup";
import Login from "./Login";

const CTA = "Get Started";

export default function Navbar() {
  return (
   <div>navbar</div>
  );
}
// top priorities signup form
// login form - authentication and testing backend
// what does the app do - how many components we need

// pages
// -profile
// main (bulletin profile(individual info))
// get rid of settings drawer
